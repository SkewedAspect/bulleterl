{port_env, [
	{"(linux|freebsd|solaris)", "LDFLAGS", "$LDFLAGS -lstdc++"}
]}.

{port_specs, [
	{"priv/bulleterl.so", ["c_src/*.c", "c_src/*.cpp"]}
]}.

% See `rebar.config.script` for the rest of the story...
